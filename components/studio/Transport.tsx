import { useState } from \"react\"\n\nexport default function Transport({ studio }) {\n  const [playing, setPlaying] = useState(false)\n  return (\n    <div className=\"p-4 border-b border-green-500/30 flex items-center space-x-4\">\n      <button\n        className=\"bg-green-500 text-white px-4 py-2 rounded\"\n        onClick={() => setPlaying(!playing)}\n        disabled={playing}\n      >\n        {playing ? \"Playing...\" : \"Play Sample\"}\n      </button>\n      <div className=\"ml-8 p-2 bg-yellow-900/30 text-yellow-200 rounded\">\n        <b>Low-Quality Preview:</b> You’ll be able to choose a high-quality version at checkout!\n      </div>\n      <label className=\"ml-4\">\n        Tempo\n        <input\n          type=\"range\"\n          min={60}\n          max={200}\n          value={studio.tempo}\n          onChange={e => studio.setTempo(Number(e.target.value))}\n          className=\"ml-2\"\n        />\n        <span className=\"ml-2 text-green-300\">{studio.tempo} BPM</span>\n      </label>\n    </div>\n  )\n}\n
