const DRUMS = [\"Kick\", \"Snare\", \"HiHat\", \"Clap\"]\n\nexport default function BeatMixer({ studio }) {\n  return (\n    <div className=\"p-4 bg-black/70 rounded mb-4\">\n      <h3 className=\"text-green-300 font-bold mb-2\">Beat Mixer</h3>\n      <div className=\"overflow-x-auto\">\n        <table>\n          <thead>\n            <tr>\n              <th></th>\n              {Array.from({ length: 16 }).map((_, i) => (\n                <th key={i} className=\"text-xs text-green-200\">{i + 1}</th>\n              ))}\n            </tr>\n          </thead>\n          <tbody>\n            {DRUMS.map((drum, drumIdx) => (\n              <tr key={drum}>\n                <td className=\"text-green-400 pr-2\">{drum}</td>\n                {studio.beatPattern.map((step, stepIdx) => (\n                  <td key={stepIdx}>\n                    <button\n                      className={`w-6 h-6 rounded ${step[drumIdx] ? \"bg-green-400\" : \"bg-gray-700\"}`}\n                      onClick={() => studio.toggleBeat(stepIdx, drumIdx)}\n                      aria-label={`Toggle ${drum} at step ${stepIdx + 1}`}\n                    />\n                  </td>\n                ))}\n              </tr>\n            ))}\n          </tbody>\n        </table>\n      </div>\n    </div>\n  )\n}\n
